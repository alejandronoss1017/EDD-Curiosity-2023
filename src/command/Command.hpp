#if !defined(COMMAND_HPP)
#define COMMAND_HPP

#include <string>

using namespace std;

class Command
{
private:
    // TODO: Implementar clase de comandos en vez de utilizar strings
    /* data */
public:
    Command(/* args */);
    ~Command();
};

#include "Command.cxx"

#endif // COMMAND_HPP
